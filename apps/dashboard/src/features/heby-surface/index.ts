/*
 * heby-surface — Heby's PRESENTATION-STATE feature (HW3).
 *
 * Which Heby surface is showing, how the Full Workspace rebalances as a conversation grows, and
 * where closing the workspace safely returns to. Presentation only: no tenant, no conversation, no
 * provider, no runtime, no authority. Every module here is pure and client-safe.
 */

export * from "./surface-state";
export * from "./composition";
