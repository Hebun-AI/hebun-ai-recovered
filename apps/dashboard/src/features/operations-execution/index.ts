/*
 * operations-execution — read-only Execution surface (Hebun UI Phase 22B). Distinguishes
 * execution activity (nothing has run) from execution capability (only READ_ONLY invokable).
 */

export * from "./contracts";
export { getExecutionSurfaceModel } from "./model";
