import { WorkspaceLanding } from "@/components/layout/workspace-landing";

export const metadata = { title: "Operations — Hebun AI" };

export default function OperationsPage() {
  return <WorkspaceLanding id="operations" />;
}
