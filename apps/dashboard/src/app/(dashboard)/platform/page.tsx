import { WorkspaceLanding } from "@/components/layout/workspace-landing";

export const metadata = { title: "Platform — Hebun AI" };

export default function PlatformPage() {
  return <WorkspaceLanding id="platform" />;
}
